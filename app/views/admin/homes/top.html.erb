<h1>管理者トップページ（注文履歴一覧画面）</h1>
<h3>注文履歴一覧</h3>
 <table border="1">
  <thead>
   <th>注文日</th>
   <th>購入者</th>
   <th>注文個数</th>
   <th>注文ステータス</th>
  </thead>

  <tbody>
   <% @orders.each do |order| %>
   <tr>
    <td><%= link_to order.created_at.strftime('%Y/%m/%d'), admin_order_path(order) %></td>
    <td><%= order.name %></td>
    <% @total = 0 %>
    <td><% order.order_details.each do |order_detail| %>
     <% @total += order_detail.amount %>
     <% end %>
     <%= @total %>
     <%#<%= order.order_detail.amount %></td>
    <td><%= order.order_status %></td>
   </tr>
   <% end %>
  </tbody>
 </table>
